package mycalculator.key;

import mycalculator.utility.KeyAllocator;

public class SquareRootKey {

	public SquareRootKey(KeyAllocator kito) {
		kito.setLabel("√");
		kito.setFunctionId(3);
		kito.setStatus(2);
	}

}
